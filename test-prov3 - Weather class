#--Testprov 3--#

#Skapa ett program som ber användaren att mata in orter och väderlekar.
#Alla orter sparas i en lista
#Användaren kan sedan välja att visa alla registrerade orter och dess väderlekar


orter = []          #Sparar alla objekt (orter)

class Weather:
    """Skpaar objekt samt funktion som displayar objektet"""

    def __init__(self, namn, temp, mm, ms):
        self.namn = namn
        self.temp = temp
        self.mm = mm
        self.ms = ms

    def display(self):
        print(self.namn,end="\t")
        print(self.temp,end="\t")
        print(self.mm,end="\t")
        print(self.ms)


def ny_ort():
    namn = input("Ange ortens namn: ")
    temp = input("Ange temperatur: ")
    mm = input("Ange nederbörd (mm): ")
    ms = input("Ange vind (m/s): ")
    print("Uppgifterna registrerade")

    namn = namn.title()                     #Ändrar till stor bokstav i början
    
    orter.append(Weather(namn, temp, mm, ms))   #Lägger till objekt i listan

def meny():

    rubrik = "VÄDERLEK"
    print("\n",rubrik)
    print("="*len(rubrik))
    print("1 Registrera ort + väderlek")
    print("2 Visa alla registrerade resultat")
    print("3 Avsluta")
    

while True:

    meny()
    val = input("Välj: ")
    print("")
    if val == "1":
        if len(orter) <= 30:
            ny_ort()
        else:
            print("Du har redan angett 30 orter")

    elif val == "2":                #om val är 2 (visa alla orter)
        if len(orter) > 0:          #Så länge det finns minst ett objekt i listan

            for i in range(len(orter)):         #Lägger alla objekt i bokstavsordning
                for j in range(i+1,len(orter)):
                    if (orter[i].namn > orter[j].namn):
                        tmp = orter[i]
                        orter[i] = orter[j]
                        orter[j] = tmp
            print("")
            print("Ort\tTemperatur\tmm\tms")
            for i in range(len(orter)):             #Skriver med "display()" ut alla
                orter[i].display()                  #objekts attachments i listan
                print("")
                
        else:
            print("Du har ännu inte angett någon ort")
    elif val == "3":
        break

    else:
        print("Fel val")
